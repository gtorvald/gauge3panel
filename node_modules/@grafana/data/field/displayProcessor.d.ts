import { Field } from '../types/dataFrame';
import { GrafanaTheme } from '../types/theme';
import { DecimalCount, DecimalInfo, DisplayProcessor } from '../types/displayValue';
import { TimeZone } from '../types';
interface DisplayProcessorOptions {
    field: Partial<Field>;
    timeZone?: TimeZone;
    theme?: GrafanaTheme;
}
export declare function getDisplayProcessor(options?: DisplayProcessorOptions): DisplayProcessor;
export declare function getDecimalsForValue(value: number, decimalOverride?: DecimalCount): DecimalInfo;
export declare function getRawDisplayProcessor(): DisplayProcessor;
export {};
